<%= form_with model: @post do |form| %>
  <p><%= form.label :title %></p>
  <p><%= form.text_field :title %></p>
  <p><%= form.label :body %></p>
  <p>
    <%= form.text_area :body%>
  </p>
  <%= form.submit "Post" %>
<% end %>