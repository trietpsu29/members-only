<%= form_with model: @post do |form| %>
  <%= form.label :title, "Title"%>
  <%= form.text_field :title %>
  <%= form.label :body, "Body"%>
  <%= form.text_area :body%>
  <%= form.submit "Post" %> 
<% end %>